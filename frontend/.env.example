# Base URL of the backend API.
# Local dev can leave this unset (defaults to http://localhost:5000).
# On Vercel, set VITE_API_BASE to your Render URL, e.g.:
#   VITE_API_BASE=https://expense-splitter-api.onrender.com
VITE_API_BASE=http://localhost:5000
